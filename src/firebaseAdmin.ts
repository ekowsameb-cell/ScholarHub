/**
 * firebaseAdmin.ts
 *
 * Firebase Admin SDK is a SERVER-ONLY module (Node.js / Cloud Functions).
 * It CANNOT be imported in a Vite/browser bundle.
 *
 * This file is kept as a reference stub for future Cloud Functions or
 * a Next.js /api route that handles admin operations (e.g., creating
 * custom auth tokens, batch writes, etc.).
 *
 * DO NOT import this file in any React component or from src/App.tsx.
 * Use the client-side `src/firebase.ts` (Firestore/Auth) from browser code.
 */

// Example server-side usage (Node.js only):
//
// import admin from 'firebase-admin';
// import serviceAccount from '../keys/serviceAccountKey.json';
//
// admin.initializeApp({
//   credential: admin.credential.cert(serviceAccount as admin.ServiceAccount),
// });
//
// export default admin;

export {};
